# Keep the build context to just the model weights.
docker-compose.yml
.env
.env.example
models/README.md

# HF download-tool bookkeeping (lock/metadata files), not part of the model
models/*/.cache/
